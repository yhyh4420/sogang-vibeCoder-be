# K-MedTour Application Configuration
# Environment variables take precedence over these values.

server.port=8080

# Database
db.url=jdbc:postgresql://localhost:5432/kmedtour
db.username=postgres
db.password=postgres

# JWT
jwt.secret=default-secret-key-for-development-only-change-in-production-32bytes
jwt.access-token-expiration=1800000
jwt.refresh-token-expiration=604800000
